# .codex

Project-scoped Codex configuration belongs here.

Keep this directory conservative:
- Do not store API keys.
- Do not store credentials.
- Do not add machine-specific secrets.
- Keep provider and telemetry settings in user-level `~/.codex/config.toml`.

Repo guidance for Codex lives in root `AGENTS.md`.
